class Compiler
    +libraries 
    +compile(code) 
    +clear() 

class Code
    +code 
    +objects(condition) 
    +insertline(number, line) 
    +updateline(number, line) 
    +removeline(number) 
    +before(position) 
    +after(position) 

class CodeObject
    +insertline(number, line) 
    +updateline(number, line) 
    +removeline(number) 
    +transferlines(other, number) 
    +isempty 

class Library
    +path 
    +loadlibraries() 

class Lexer
    +setitem(key, value) 
    +getitem(key) 
    +registerhandlers(handlers) 
    +unregisterhandlers() 

class Logger
    +getLogger(name) 
    +info(msg, object) 
    +warning(msg, object) 
    +error(msg, object) 
    +critical(msg, object) 
    +getevents(levels) 
    +has(levels) 

class Event
    +msg 
    +level 

class Parser
    +parse(code_objects) 

class DependencyResolver
    +resolve() 

class Validator
    +validate() 

class UMLObject
    +required 
    +allowed 
    +type 
    +parent 
    +name 
    +isprototype 
    +codeobject 
    
    +setitem(key, value) 
    +getitem(key) 

    +addoperation(operation) 
    +addattribute(attribute) 

    +operations() 
    +attributes() 
    
class Field
    +isstatic 
    +visibility 
    +name 
    +type 

class Operation
    +parameters 

class Atribute
    +defaultvalue 

prototype aggregation umlo-aggregation
    source-object : UMLObject

generalization 
    source-object : Library
    target-object : Code

aggregation
    source-object : CodeObject
    target-object : Code
    target-count  : 1..*

composition 
    source-object : Lexer
    target-object : Code

composition 
    source-object : Compiler
    target-object : Parser

composition 
    source-object : Compiler
    target-object : Resolver
    
composition 
    source-object : Compiler
    target-object : Library
    target-count  : 1..*

umlo-aggregation 
    target-object : Compiler

aggregation 
    source-object : Event
    target-object : Logger

composition 
    source-object : UMLObject
    target-object : Event
    target-count  : 0..1

composition 
    source-object : Parser
    target-object : Lexer

umlo-aggregation 
    target-object : Parser

umlo-aggregation 
    target-object : Resolver

aggregation 
    source-object : Field
    target-object : UMLObject

generalization 
    source-object : UMLOperation
    target-object : Field

generalization 
    source-object : UMLAttribute
    target-object : Field

composition 
    source-object : CodeObject
    target-object : UMLObject

umlo-aggregation 
    target-object : Validator
