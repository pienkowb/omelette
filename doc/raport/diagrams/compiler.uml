class Compiler
    +libraries 
    +compile(code) 
    +clear() 

class Code
    +code 
    +objects(condition) 
    +insertline(number, line) 
    +updateline(number, line) 
    +removeline(number) 
    +before(position) 
    +after(position) 

class CodeObject
    +insertline(number, line) 
    +updateline(number, line) 
    +removeline(number) 
    +transferlines(other, number) 
    +isempty 

class Library
    +path 
    +loadlibraries() 

class Lexer
    +setitem(key, value) 
    +getitem(key) 
    +registerhandlers(handlers) 
    +unregisterhandlers() 

class Logger
    +getLogger(name) 
    +info(msg, object) 
    +warning(msg, object) 
    +error(msg, object) 
    +critical(msg, object) 
    +getevents(levels) 
    +has(levels) 

class Event
    +msg 
    +level 

class Parser
    +parse(code_objects) 

class DependencyResolver
    +resolve() 

class Validator
    +validate() 

class UMLObject
    +required 
    +allowed 
    +type 
    +parent 
    +name 
    +isprototype 
    +codeobject 
    
    +setitem(key, value) 
    +getitem(key) 

    +addoperation(operation) 
    +addattribute(attribute) 

    +operations() 
    +attributes() 
    
class Field
    +isstatic 
    +visibility 
    +name 
    +type 

class Operation
    +parameters 

class Attribute
    +default_value 

prototype aggregation umlo-aggregation
    target-object : UMLObject

generalization 
    source-object : Library
    target-object : Code

aggregation
    source-object : Code
    target-object : CodeObject
    target-count  : 1..*

composition 
    source-object : Code
    target-object : Lexer

composition 
    source-object : Compiler
    target-object : Parser

composition 
    source-object : Compiler
    target-object : DependencyResolver
    
composition 
    source-object : Compiler
    target-object : Library
    target-count  : 1..*

umlo-aggregation 
    source-object : Compiler

aggregation 
    source-object : Logger
    target-object : Event

composition 
    source-object : Event
    target-object : UMLObject
    target-count  : 0..1

composition 
    source-object : Parser
    target-object : Lexer

umlo-aggregation 
    source-object : Parser

umlo-aggregation 
    source-object : DependencyResolver

aggregation 
    source-object : UMLObject
    target-object : Field

generalization 
    source-object : Operation
    target-object : Field

generalization 
    source-object : Attribute
    target-object : Field

composition 
    source-object : UMLObject
    target-object : CodeObject

umlo-aggregation 
    source-object : Validator

association
    source-object : Compiler
    target-object  : Validator

association
    source-object : Compiler
    target-object  : Logger    